const intialState = {
  currentUser: null,
};

// const LOAD_USER = 'auth/current_user';

const reducer = (state = intialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
